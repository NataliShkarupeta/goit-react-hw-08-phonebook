import { createSlice } from "@reduxjs/toolkit";

const regisrerSlice = createSlice({
  name: 'isRegister',
  initialState: {
    isRegister:false,

  },
  reducers:{

  }
});

export default regisrerSlice.reducer